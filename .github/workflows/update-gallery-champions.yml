name: Update Gallery-champions

on:
  push:
    paths:
      - 'jpg/uploads/champions/**'
  workflow_dispatch:

jobs:
  update-gallery:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate gallery-champions.js
        run: |
          echo "const images = [" > gallery-champions.js
          find jpg/uploads/champions -type f \( -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.png" -o -iname "*.gif" -o -iname "*.webp" \) \
          | sort -r \
          | sed 's/^/  \"/; s/$/\",/' >> gallery-champions.js
          echo "];" >> gallery-champions.js

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add gallery-champions.js
          git commit -m "Update gallery-champions.js automatically" || echo "No changes to commit"
          git push
