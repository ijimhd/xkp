name: Update Gallery-super

on:
  push:
    paths:
      - 'jpg/uploads/super/**'
  workflow_dispatch:

jobs:
  update-gallery:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate gallery-super.js
        run: |
          echo "const images = [" > gallery-super.js
          find jpg/uploads/super -type f \( -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.png" -o -iname "*.gif" -o -iname "*.webp" \) \
          | sort -r \
          | sed 's/^/  \"/; s/$/\",/' >> gallery-super.js
          echo "];" >> gallery-super.js

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add gallery-super.js
          git commit -m "Update gallery-super.js automatically" || echo "No changes to commit"
          git push
